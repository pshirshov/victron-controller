# M-AUDIT Execution Plan — 2026-04-24

Read-only planning artefact. Executors consume this + `defects.md` + code.
Do not re-open the audit files under `/tmp/review-*.md`.

## Questions for the user (answered inline)

Five design calls were surfaced by the planner. Answers applied here so
Wave 1 can start immediately; blocked PRs pick up these defaults.

1. **A-13 + A-14 `zappi_limit` unit (%/kWh):** DEFERRED to M-AUDIT-2.
   Both audits are blocked on this decision; neither is in the initial
   12-PR batch.
2. **A-10 / PR-09 `grid_import_limit_w` default:** **10 W** (matches the
   idle-bleed promotion the user already asked for in
   [-5000, 10] W range, which is configurable).
3. **A-09 / PR-09 `grid_export_limit_w` SAFE_MAX ingest clamp:** **10_000 W**
   (10 kW). Default value remains `4900` in `safe_defaults`; `SAFE_MAX`
   is the acceptance ceiling for retained MQTT / dashboard writes.
4. **Cross-cutting `charge_battery_extended` source of truth:** option
   (a) — **derivation in `run_schedules` wins**. Weather-SoC writes a
   separate bookkeeping field `charge_battery_extended_today` that
   resets at midnight. Matches the TASS "derived view" principle.
5. **A-19 / PR-10 `force_disable_export` in `CurrentLimitInputGlobals`:**
   option (a) — **delete the field**. Matches the project's "delete,
   don't pretend" hygiene.

---

## PR-01 — `extract_scalar` NaN/Inf/Bool filter [not blocked]

**Scope:** resolves A-01, A-02; partial A-48, A-51, A-41.

**File + line targets:**
- `crates/shell/src/dbus/subscriber.rs:436-457` — `extract_scalar` body.
- `crates/shell/src/dbus/subscriber.rs` `#[cfg(test)] mod tests` — add tests.

**D-steps:**
- D01. Replace `Value::F64(f) => Some(*f)` with `Value::F64(f) if f.is_finite() => Some(*f)`.
- D02. Delete `Value::Bool(b)` arm entirely.
- D03. Add a fall-through `Value::F64(_) => None` (non-finite is a sensor
  dropout, not data).
- D04. Add tests: NaN → None; ±Inf → None; finite negative → Some(-x);
  `Value::Bool(true)` → None.
- D05. `cargo test -p victron-controller-shell dbus::`.

**Acceptance:**
- A-01, A-02 → `resolved` in defects.md.
- `cargo test --all` green; `cargo clippy --all-targets -- -D warnings`
  green; ARMv7 cross-compile green.
- New test: `extract_scalar_rejects_nonfinite_and_bool`.

**Risk / blast radius:** A sensor previously served as `Bool` would now
go permanently Stale. `ess_state` is the only bool-adjacent — published
as `Int` on Venus — safe. No Decision output shape change, no wire
protocol change, no knob default change.

**Observer-mode preservation:** unchanged (ingest boundary only).

**Rollback trigger:** `battery_soc` or `grid_power` permanently Stale on
the dashboard within 30 s of deploy.

---

## PR-02 — Grid-voltage ÷0 guard in `current_limit` [not blocked]

**Scope:** resolves A-03.

**File + line targets:**
- `crates/core/src/controllers/current_limit.rs:141, 176, 188` — three
  `/ grid_voltage` sites.
- Same file, top — add `MIN_SENSIBLE_GRID_V: f64 = 180.0;`,
  `NOMINAL_GRID_V: f64 = 230.0;`.
- Same file, tests section — add property test.

**D-steps:**
- D01. Add the two constants near existing controller constants.
- D02. Introduce `fn effective_grid_v(measured: f64) -> (f64, bool)`
  returning `(230.0, true)` if `< MIN_SENSIBLE_GRID_V` or non-finite,
  else `(measured, false)`. Flag indicates fallback.
- D03. At each `/grid_voltage` site, compute `(v_eff, fell_back)` and
  divide by `v_eff`. Thread `fell_back` into a new Decision factor
  `grid_v_fallback`.
- D04. Tests: `grid_voltage=0.0` → finite result + decision factor
  present; healthy `grid_voltage=240` → no fallback flag.

**Acceptance:**
- A-03 → `resolved`.
- Cargo test/clippy/cross-compile green.
- New test: `current_limit_grid_v_fallback_on_grid_loss`.

**Risk / blast radius:** new Decision factor line — dashboard renders it.
No output-shape change, no wire change, no knob default change.

**Observer-mode preservation:** unchanged.

**Rollback trigger:** "grid_v fallback" factor always-on on a healthy
install (threshold wrong).

---

## PR-03 — Zappi `time_in_state` monotonic-Instant fix [not blocked]

**Scope:** resolves A-04, A-24.

**File + line targets:**
- `crates/shell/src/myenergi/types.rs:39-44, 66, 107-113` — remove
  `parse_myenergi_ts` fallbacks; make the whole parse fail on error.
- `crates/core/src/myenergi.rs` (or equivalent `ZappiState` location in
  core) — store `last_observed_change: Option<Instant>` alongside the
  signature hash.
- `crates/shell/src/myenergi/mod.rs` — poller stamps `Instant::now()`
  when signature differs from previous observation.
- `crates/core/src/controllers/current_limit.rs:153-154` — consume the
  `Instant` via `clock.monotonic().saturating_duration_since(...)`.

**D-steps:**
- D01. Introduce `ZappiObservation { state, signature_hash, observed_at: Instant }`
  at the shell layer.
- D02. Shell poller caches last-seen `signature_hash`; on change, stamps
  `observed_at = Instant::now()`; else reuses prior value.
- D03. Extend `TypedReading::Zappi` to carry `observed_at: Instant`.
- D04. Core stores the `Instant` in `world.typed_sensors.zappi_state`.
- D05. `current_limit.rs:153-154` uses the `Instant` for
  `time_in_state_min`.
- D06. Remove `unwrap_or("01-01-2026")` / `unwrap_or("00:00:00")` and the
  sentinel fallback in `parse_myenergi_ts`; parse failures return
  `None` from `parse_zappi` (ties in A-24).
- D07. Test: `zappi_time_in_state_is_monotonic_across_dst_fallback`.

**Acceptance:**
- A-04, A-24 → `resolved`.
- Tests green on `cargo test --workspace`; cross-compile green.

**Risk / blast radius:** `TypedReading::Zappi` shape change; callers
across shell + core must update. MQTT payload for `ZappiState` stays
wall-clock (observability); the `Instant` is core-only.

**Observer-mode preservation:** unchanged (bookkeeping timestamp only).

**Rollback trigger:** `current_limit` stuck `zappi_active=false` in BST.

---

## PR-04 — Controller ordering + `charge_to_full_required` latch fix

**Scope:** resolves A-05, A-15; partial A-18.

Uses answer Q4: derivation wins, weather_soc writes a separate
`charge_battery_extended_today` with daily reset.

**File + line targets:**
- `crates/core/src/process.rs:406-418` — `run_controllers` dispatch.
- Same file — new helper `compute_derived_view(world) -> DerivedView`.
- Same file, `run_setpoint` reads `zappi_active` from `DerivedView` not
  `bookkeeping.zappi_active`.
- Same file, `run_weather_soc` (lines `1054-1077, 1085-1093`): replace
  `charge_to_full_required |= ...` with a write to the new
  `charge_battery_extended_today` field.
- `crates/core/src/world.rs` — `Bookkeeping` gets
  `charge_battery_extended_today: bool` and
  `last_weather_soc_run_date: Option<NaiveDate>`.
- `crates/core/src/process.rs:698-705` — schedules derivation reads
  `bk.charge_battery_extended_today` + existing weekly rollover.
- `crates/core/src/process.rs` `apply_tick` — add midnight reset of
  `charge_battery_extended_today`.

**D-steps:**
- D01. Define `DerivedView { zappi_active: bool, ... }`.
- D02. `run_controllers` calls `compute_derived_view` once at entry.
- D03. Thread `&DerivedView` into `run_setpoint`; consume from there.
- D04. Extend `Bookkeeping` with the two new fields.
- D05. Replace the `|=` in weather_soc with a direct set of
  `charge_battery_extended_today`.
- D06. Update `run_schedules` `charge_battery_extended` derivation to
  also OR in `bk.charge_battery_extended_today`.
- D07. Add midnight reset: in `apply_tick`, if `now.date()` !=
  `bk.last_weather_soc_run_date`, set
  `charge_battery_extended_today = false` and clear
  `last_weather_soc_run_date`.
- D08. Tests: `charge_to_full_required_resets_after_midnight_if_weekly_not_active`,
  `setpoint_first_tick_sees_derived_zappi_active`.

**Acceptance:**
- A-05, A-15 → `resolved`. A-18 partially addressed (derivation path).
- Cargo test/clippy/cross-compile green.

**Risk / blast radius:** Bookkeeping shape change → retained-MQTT
bookkeeping topic gains new fields. Add migration-on-ingest: unknown
fields dropped; new ones default-initialised. Cross-controller
invariant: setpoint reads `DerivedView.zappi_active`; current_limit can
still write `bk.zappi_active` for display/observability.

**Observer-mode preservation:** unchanged.

**Rollback trigger:** setpoint oscillates after a cold boot; or
`charge_to_full_required` stuck `true` past midnight on a non-Sunday.

---

## PR-05 — Observer → live transition: target invalidation [not blocked]

**Scope:** resolves A-06, A-07, A-59.

**File + line targets:**
- `crates/core/src/process.rs` all `maybe_propose_*` (setpoint,
  current_limit, schedules, zappi_mode, eddi_mode).
- `crates/core/src/process.rs:257-260` — `Command::KillSwitch` arm.
- `crates/core/src/tass/actuated.rs` — add `reset_to_unset(at)`.
- `crates/core/src/world.rs` or `knob_provenance.rs` — track
  `prev_writes_enabled: bool` on the edge.

**D-steps:**
- D01. Add `Actuated::reset_to_unset(&mut self, at: Instant)` that
  resets `target = Target::unset(at)` without touching actual
  freshness.
- D02. In every `maybe_propose_*`: when `writes_enabled=false`, SKIP
  `propose_target` and `ActuatedPhase` publish. Emit only the
  `Effect::Log`.
- D03. On `Command::KillSwitch(true)` where `prev=false`, call
  `reset_to_unset(at)` on every actuated target. Publish
  `ActuatedPhase { phase: Unset }` for each so the UI reflects reality.
- D04. Track `prev_writes_enabled` somewhere (suggestion: a
  `bool` field on `Bookkeeping` with semantics "what the last
  `KillSwitch` command set").
- D05. Test: observer→live→observer→live cycle; `writes_enabled=false`
  while controller wants `10 W`; flip `true`; next `Tick` emits
  `WriteDbus{10 W}`.
- D06. Test: `kill_switch_true_resets_pending_targets_forces_rewrite_next_tick`.

**Acceptance:**
- A-06, A-07, A-59 → `resolved`.
- Cargo test/clippy/cross-compile green.

**Risk / blast radius:** Behavioural change. First post-flip tick
forces writes across all actuators — expected, visible in logs.

**Observer-mode preservation:** This PR RESTORES the invariant the
honesty audits found broken. Cold-start `writes_enabled=false` still
emits zero `WriteDbus`; three-layer chain untouched.

**Rollback trigger:** burst of 6 writes on every tick after
`writes_enabled=true` (reset edge-detection broken, firing every tick).

---

## PR-06 — MQTT retained-knob validation [not blocked]

**Scope:** resolves A-08; partial A-61; optional ride-along A-49.

**File + line targets:**
- `crates/shell/src/mqtt/serialize.rs:263-332` — `parse_knob_value`.
- Same file `:59-112` — bootstrap ingest.
- Same file `:290` — DischargeTime format.
- `crates/core/src/process.rs:363-367` — `apply_knob` catch-all.
- `crates/shell/src/mqtt/discovery.rs` — existing knob range table
  becomes the shared source.

**D-steps:**
- D01. Extract a `KnobRange` table (const or fn) with per-knob
  `min/max/enum-members`. Used by both discovery and parse validation.
- D02. In `parse_knob_value`, for every float path add
  `.filter(|f| f.is_finite())` plus a range check. Out-of-range →
  `None` + `warn!("retained knob out of range: …")`.
- D03. Bootstrap ingest `:59-112`: skip `None` from `parse_knob_value`
  (don't promote to `Owner::System`).
- D04. `apply_knob` catch-all: change silent drop to
  `warn!("knob schema drift: id={id:?} value={value:?}")` — A-61
  resolved.
- D05. Ride-along (optional in-scope): A-49 — accept both `HH:MM` and
  `HH:MM:SS` in DischargeTime parser.
- D06. Tests: `parse_knob_value_rejects_nan_inf_out_of_range`,
  `bootstrap_skips_invalid_retained_knobs`.

**Acceptance:**
- A-08, A-61 → `resolved`. A-49 → `resolved` if ride-along included.
- Cargo test/clippy/cross-compile green.

**Risk / blast radius:** Boot behaviour change — poisoned retained
values no longer silently applied. Desired outcome; document in PR
description. No wire protocol change; no knob default change.

**Observer-mode preservation:** `writes_enabled` itself passes through
the same path — bogus retained values fall back to `false` cold-start
default, matching G3 safe. Invariant preserved.

**Rollback trigger:** boot log floods with "retained knob out of range"
on a healthy broker (range table wrong).

---

## PR-07 — `GetNameOwner` re-resolution on `NameOwnerChanged` [not blocked]

**Scope:** resolves A-11.

**File + line targets:**
- `crates/shell/src/dbus/subscriber.rs:226-254` — owner map build.
- Same file, subscriber main loop — add `NameOwnerChanged` handler.

**D-steps:**
- D01. Subscribe to `org.freedesktop.DBus.NameOwnerChanged` signals,
  filtered to known well-known names.
- D02. Single-threaded subscriber keeps `owner_to_service` as a local
  `HashMap`; `NameOwnerChanged { name, new_owner }` updates the entry.
  Empty `new_owner` → remove entry (service gone).
- D03. Unit test on the mapping update pure function.

**Acceptance:**
- A-11 → `resolved`.
- Cargo test/clippy/cross-compile green.
- New test: `name_owner_changed_updates_mapping`.

**Risk / blast radius:** Slight signal volume bump. Filter aggressively
on well-known-name membership to avoid processing unrelated events.

**Observer-mode preservation:** unchanged.

**Rollback trigger:** CPU spike on startup (filter too broad).

---

## PR-08 — `SchedulePartial` accumulator clearing [not blocked]

**Scope:** resolves A-12; partial A-57 (readback side).

**File + line targets:**
- `crates/shell/src/dbus/subscriber.rs:141-172, 360-371`.

**D-steps:**
- D01. Change `SchedulePartial` to track a `HashSet<FieldName>` of
  fields observed-since-last-emit.
- D02. Emit a `Schedule0/1` readback ONLY when all five fields appear
  in the set. On emit, clear the set.
- D03. Test: single-field `ItemsChanged` after seed does NOT emit a
  readback.

**Acceptance:**
- A-12 → `resolved`. A-57 noted in PR description as partially
  addressed (readback-side); writer-side atomic burst deferred.
- Cargo test/clippy/cross-compile green.
- New test: `schedule_partial_requires_full_batch_before_readback`.

**Risk / blast radius:** TASS `Confirmed` phase now arrives ≥ one poll
cycle later. `setpoint_retarget_deadband` insulates; no regression
expected.

**Observer-mode preservation:** unchanged.

**Rollback trigger:** schedule target stuck in `Commanded` forever.

---

## PR-09 — `grid_export_limit_w` safety + `grid_import_limit_w` knob

**Scope:** resolves A-09, A-10, A-31, A-34. Optional ride-along: A-35.

Uses Q2 + Q3 defaults: `grid_import_limit_w` default 10 W, ingest
clamps at 10_000 W for both export and import.

**File + line targets:**
- `crates/core/src/process.rs:470-471` — grid_cap construction.
- `crates/core/src/process.rs:513-516` — deadband i32 subtraction.
- `crates/core/src/knobs.rs` — add `grid_import_limit_w: u32`
  (default `10`). Add `SAFE_MAX_GRID_LIMIT_W = 10_000`.
- `crates/core/src/types.rs` — `KnobId::GridImportLimitW`.
- `crates/shell/src/mqtt/serialize.rs` — parse new knob; range check
  via the PR-06 `KnobRange` table (PR-09 merges after PR-06).
- `crates/shell/src/mqtt/discovery.rs` — HA discovery entry for new
  knob.
- `crates/shell/src/dashboard/convert.rs:417, 421` — truncation fixes.
- `SPEC.md` §7 — add row for new knob + document SAFE_MAX semantics.

**D-steps:**
- D01. Add `grid_import_limit_w: u32` knob; default=10.
- D02. Define `SAFE_MAX_GRID_LIMIT_W: u32 = 10_000` near other topology
  constants.
- D03. In `run_setpoint` post-processing: compute
  `export_cap = k.grid_export_limit_w.min(SAFE_MAX_GRID_LIMIT_W) as i32`
  and `import_cap = k.grid_import_limit_w.min(SAFE_MAX_GRID_LIMIT_W) as i32`.
  Clamp both sides: `capped = target.clamp(-export_cap, import_cap)`.
- D04. Add two Decision factors for honesty:
  `pre_clamp_setpoint_W={out.setpoint_target}`,
  `clamp_bounds=[-{export_cap}, {import_cap}] W`,
  `post_clamp_setpoint_W={capped}`.
- D05. Deadband overflow fix (A-31): `i64::from(a) - i64::from(b)`
  before `.abs()`.
- D06. Dashboard convert (A-34): `i32::try_from(...).unwrap_or(i32::MAX)`.
  Same fix for `eddi_dwell_s` (A-35 ride-along) if keeping in scope.
- D07. Tests: `grid_export_limit_pathological_u32_clamped_to_safe_max`,
  `grid_import_clamp_defaults_to_10_W_on_positive_decision`,
  `setpoint_deadband_does_not_overflow_at_extremes`.
- D08. SPEC §7: add row for `grid_import_limit_w` with default 10,
  SAFE_MAX note.

**Acceptance:**
- A-09, A-10, A-31, A-34 → `resolved`. A-35 → `resolved` if included.
- Cargo test/clippy/cross-compile green.

**Risk / blast radius:** New knob → new MQTT topic + HA entity; retained
bootstrap will see a missing knob → defaults apply (safe). Decision
shape changes (two new factors) — dashboard renders them.

**Observer-mode preservation:** unchanged (clamp is inside the
already-gated `maybe_propose_setpoint`).

**Rollback trigger:** setpoint oscillates 0↔10 W on a production
install (idle-promotion interacting with upstream logic).

---

## PR-10 — Delete unused `force_disable_export` field [not blocked]

**Scope:** resolves A-19.

Uses Q5: delete the field.

**File + line targets:**
- `crates/core/src/controllers/current_limit.rs:62, 263` — field
  declaration + any fixture initialisers.
- `crates/core/src/process.rs:608` (or equivalent) — population site.
- `crates/core/tests/*.rs` — test fixtures that set the field.

**D-steps:**
- D01. Remove the field from `CurrentLimitInputGlobals`.
- D02. Remove the population site in `process.rs`.
- D03. Remove test fixtures that set it.
- D04. Confirm setpoint still consults `k.force_disable_export`
  directly — unchanged.

**Acceptance:**
- A-19 → `resolved`.
- Cargo test/clippy/cross-compile green.

**Risk / blast radius:** Smallest PR. Struct shape change within core.

**Observer-mode preservation:** unchanged.

**Rollback trigger:** setpoint `force_disable_export` branch behaves
differently post-merge (shouldn't — it reads `k.` directly).

---

## PR-11 — Weather-SoC γ-hold + once-per-day + A-36 ride-along

**Scope:** resolves A-20, A-21, A-36.

Couples with PR-04 (`last_weather_soc_run_date` is added there).

**File + line targets:**
- `crates/core/src/process.rs:980` — `run_weather_soc` entry.
- `crates/core/src/process.rs:1054-1077, 1085-1093` — direct
  `apply_knob` calls.
- `crates/core/src/process.rs:963-965` — A-36:
  `eddi_last_transition_at` update moved above `writes_enabled` gate.

**D-steps:**
- D01. Gate `run_weather_soc` body on "once per wall-day within the
  01:55 window": if `last_run_date == today` AND we're still in that
  window, log + return early.
- D02. Replace direct `apply_knob(...)` calls with
  `Command::Knob { owner: Owner::WeatherSocPlanner, ... }` routed
  through `accept_knob_command`.
- D03. Extend `accept_knob_command` γ-hold to also suppress
  `WeatherSocPlanner` within the dashboard hold window (not only
  `HaMqtt`).
- D04. A-36: move `eddi_last_transition_at = Some(now)` update above
  the `if !writes_enabled` gate in `run_eddi_mode`. Bookkeeping-only;
  safe under observer mode.
- D05. Tests: `weather_soc_runs_once_per_day`,
  `weather_soc_respects_dashboard_gamma_hold`,
  `eddi_dwell_clock_advances_in_observer_mode`.

**Acceptance:**
- A-20, A-21, A-36 → `resolved`.
- Cargo test/clippy/cross-compile green.

**Risk / blast radius:** Reduces retained-MQTT weather_soc messages from
~300/day to ~5. HA users see fewer events; document. γ-hold now
blocks `WeatherSocPlanner` too — ensure this doesn't silently
permanently suppress if a Dashboard write goes into the future
accidentally.

**Observer-mode preservation:** A-36 ride-along **strengthens** the
invariant (dwell clock becomes honest in shadow run).

**Rollback trigger:** weather_soc never runs (date check inverted) or
γ-hold permanently suppresses it.

---

## PR-12 — myenergi HTTP body-level error parsing [not blocked]

**Scope:** resolves A-22, A-23.

**File + line targets:**
- `crates/shell/src/myenergi/mod.rs:116-144` — `set_zappi_mode` /
  `set_eddi_mode`.
- Same file `:117-119, 134-135` — empty-credentials guard (A-23).
- `crates/shell/src/runtime.rs` — effect dispatcher that publishes
  `ActuatedPhase{Unset}` on error.

**D-steps:**
- D01. Parse response body JSON; require `zsh=0` (zappi) /
  equivalent for eddi.
- D02. Non-zero body code → `Err(MyenergiError::Rejected{code})`.
- D03. Empty credentials → `Err(MyenergiError::NotConfigured)`
  (previously silent `Ok`).
- D04. In runtime/writer dispatcher, on any `Err`, publish
  `ActuatedPhase{Unset}` + `warn!`.
- D05. Test: mock 200 with `{"zsh":3}` → `Err`; phase flips to
  `Unset`.

**Acceptance:**
- A-22, A-23 → `resolved`.
- Cargo test/clippy/cross-compile green.
- New test: `myenergi_body_level_error_is_not_success`.

**Risk / blast radius:** UI state change — "Commanded" → "Unset" on
device rejection. Correct but visible to users.

**Observer-mode preservation:** unchanged (gate is upstream in
`runtime.rs`).

**Rollback trigger:** every myenergi write errors post-deploy (body
shape wrong).

---

## Deferred to M-AUDIT-2

A-13, A-14, A-16, A-17 (full), A-18 (remainder), A-25, A-26, A-27, A-28,
A-29, A-30, A-32, A-33, A-35 (if not ride-along), A-37, A-38, A-39,
A-40, A-41 (beyond PR-01 scope), A-42, A-43, A-44, A-45, A-46, A-48
(forecast `as_f64`), A-49 (if not ride-along), A-50, A-51, A-52, A-53,
A-54, A-55, A-56, A-57 (writer side), A-58, A-60, A-62, A-63, A-64,
A-65, A-67, A-68.

Each has a one-line deferral reason in defects.md.

---

## Execution order

### Wave 1 — independent physical-safety foundations (parallel)

- **PR-01** (subscriber.rs) — NaN filter at ingest.
- **PR-02** (current_limit.rs) — div-zero guard.

### Wave 2 — observer/live honesty + retained-state sanitisation (serial after W1)

- **PR-05** (process.rs) — observer→live invariant.
- **PR-06** (mqtt/serialize.rs) — retained-knob validation. Parallel to
  PR-05 (disjoint files).

### Wave 3 — sensor plumbing + bus recovery (parallel after W2)

- **PR-03** (myenergi/types.rs + mod.rs, core/myenergi.rs,
  controllers/current_limit.rs) — Zappi monotonic Instant.
- **PR-07** (dbus/subscriber.rs) — NameOwnerChanged.
- **PR-08** (dbus/subscriber.rs) — SchedulePartial. Serial after PR-07
  (same file).

### Wave 4 — structural ordering + latch (serial)

- **PR-04** (process.rs, world.rs) — DerivedView + `charge_battery_extended_today`.
- **PR-11** (process.rs) — weather_soc once-per-day + γ-hold + A-36
  ride-along. Serial after PR-04 (same file; PR-04 adds the bookkeeping
  field PR-11 consumes).

### Wave 5 — knob safety + myenergi body errors + dead-field removal (parallel)

- **PR-09** (process.rs, knobs.rs, types.rs, discovery.rs, serialize.rs,
  convert.rs, SPEC.md) — new knob + clamp. Serial after PR-06 (reuses
  its `KnobRange` table).
- **PR-10** (controllers/current_limit.rs, process.rs) — delete dead
  field.
- **PR-12** (myenergi/mod.rs, runtime.rs) — body-error parsing.

---

## Cross-PR invariants to re-check at merge

- Every `maybe_propose_*` still emits a Decision (honesty invariant).
- Three-layer gate chain unchanged: `knobs.writes_enabled` (PR-05
  touches), config `[dbus] writes_enabled`, config `[myenergi]
  writes_enabled`.
- Cold-start `writes_enabled=false` preserved.
- No wire-protocol breakage: PR-03 Instant is core-only; PR-04
  bookkeeping fields additive; PR-09 new knob topic additive.
- Every PR passes: `cargo test --all`, `cargo clippy --all-targets
  -- -D warnings`, `cargo build --target
  armv7-unknown-linux-gnueabihf --release`.
