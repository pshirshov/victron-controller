# Session log — 2026-04-24 late evening (M-AUDIT-2 review-loop drain)

## User direction

"Deployed, sensors behave healthy! I guess you could continue with the
rest of the problems in /review-loop . schedule 0 is still disabled -
a blocker!"

Field sensors stable after PR-CADENCE + reconnect work. User flagged
the schedule_0 "disabled" display as a blocker. Then asked to continue
the review-loop on remaining M-AUDIT-2 defects.

## Schedule_0 blocker — root cause was the Decision *text*, not the controller

Three-part resolution:

1. **PR-UX-1 (`bf7b596`)** — dashboard `render.ts` rendered
   ScheduleSpec as `JSON.stringify(...)`. User correctly read
   `{"days":-7}` as "disabled" in the actual column. Replaced with
   explicit `ENABLED 02:00–05:00 soc=80%` / `DISABLED ...` labels
   styled green/red.

2. **PR-sched-decisions (`5f92960`)** — the Decision panel was the
   actual blocker. `evaluate_schedules` returned one Decision;
   `run_schedules` copied it into both `world.decisions.schedule_0`
   AND `world.decisions.schedule_1`. The branch text was written
   about Schedule 1 ("no extended-charge flags set → Schedule 1
   disabled") but displayed on both rows. Split `SchedulesOutput`
   into `schedule_0_decision` (the "unconditionally ENABLED"
   invariant) and `schedule_1_decision` (branch-specific). cbe
   factors now attached only to schedule_1 where they actually gate.

## Commits this session

| Commit | PR | Summary |
|---|---|---|
| `bf7b596` | PR-UX-1 | `render.ts` schedule rows → `ENABLED ... / DISABLED ...` instead of raw JSON. |
| `aab6c28` | PR-03 (A-04 + A-24) | `zappi_last_change_signature` → monotonic `Instant` stamped by poller on state change. Deletes `parse_myenergi_ts` sentinel fallback. Kills the BST/DST-offset bug and the sentinel-date change-detection blindness together. |
| `5f92960` | PR-sched-decisions | Split Schedule 0 / Schedule 1 decisions. schedule_0 gets "unconditionally ENABLED" text; schedule_1 keeps branch-specific text + cbe factors. Test assertions updated to read schedule_1's decision. |
| `3d9c987` | PR-11 (A-20 + A-21) | Weather-SoC γ-hold: `accept_knob_command` now suppresses `Owner::WeatherSocPlanner` within the dashboard hold window. Once-per-day guard via `Bookkeeping::last_weather_soc_run_date`. `propose_knob` rewrites to take `(owner, at)` and gate through `accept_knob_command`. 2 new tests pin behavior. |
| `61cfee8` | PR-forecast-freshness (A-16) | `ControllerParams::freshness_forecast: Duration` (default 12 h). `run_weather_soc` builds a real `is_fresh` closure from `clock.monotonic()` vs `snap.fetched_at`. "Skipped" Decision now explains why + surfaces the threshold. |
| `a925147` | PR-HYGIENE-2 | A-25 (u8 truncation → `try_from`), A-41 (NaN in forecast_fusion → filter), A-48 (`as_f64` accepts NaN/Inf strings → filter), A-51 (myenergi `che` → is_finite + non-negative). |
| `ad8b25c` | PR-solcast-schema (A-26) | Solcast fetch counts `total_items` vs `parsed_items`; bails with explicit error on schema drift instead of silently emitting 0 kWh. |
| `1afc73d` | PR-forecast-backoff (A-28) | `FetchFailure` enum (AuthFailed / RateLimited / ServerError / Other) + scheduler-level response: auth → exit fetcher, 429 → 6 h backoff, 5xx → exponential 60 s → 30 min, other → normal cadence. Scoped to forecast; myenergi scope-deferred. |

## M-AUDIT-2 defects resolved this session

- A-04, A-24 (Zappi timestamp → Instant)
- A-16 (forecast freshness)
- A-20, A-21 (weather_soc γ-hold + once-per-day)
- A-25 (u8 truncation)
- A-26 (Solcast schema drift)
- A-28 (HTTP status classification + backoff, forecast side)
- A-41 (forecast_fusion NaN)
- A-48 (`as_f64` NaN/Inf string)
- A-51 (myenergi `che` unwrap)

Plus the schedule blocker (not tracked as A-NN but user-critical).

Combined with the previous evening session:
- A-04, A-11, A-12, A-16, A-19, A-20, A-21, A-22, A-23, A-24, A-25,
  A-26, A-28, A-36, A-40, A-41, A-42, A-48, A-51, A-54 now closed.

## Remaining M-AUDIT-2 backlog (not yet addressed)

### Major / meaningful
- **A-09** (grid_export_limit_w > i32::MAX silently disables cap) / **A-10** (=0 pins setpoint at 0) — PR-09b bundle.
- **A-13** (session_charged_pct hardcoded 0 in run_zappi_mode) — blocks on **A-14** (zappi_limit % vs kWh design call).
- **A-17** (Hoymiles export not folded into solar_export).
- **A-27** (Solcast `period_end` boundary misattribution) — minor systematic shift.
- **A-28** myenergi side (status classification + backoff for myenergi HTTP).
- **A-29** / **A-57** / **A-65** — Schedule-writer atomicity + Venus firmware variance.
- **A-30** (event channel watermark) — likely subsumed by main.rs:60-88 warning; audit.
- **A-31** / **A-47** (i32 deadband overflow — likely resolved by PR-09a, verify).
- **A-33** (PV multiplier float-equality ladder).
- **A-34** / **A-35** (u32 → i32 truncation).
- **A-39** (Dashboard writes_enabled badge only reads core flag).
- **A-43** (Open-Meteo hidden efficiency multiplier 0.75).
- **A-44** (HA discovery max=500 vs SPEC 1000).
- **A-45** (Topology cadence comment stale after PR-CADENCE — quick doc fix).
- **A-46** (evening discharge + zappi can net-import at peak).
- **A-50** (forecast TZ drift on Venus UTC install).
- **A-52** (mqtt rand_suffix collision — use uuid v4).
- **A-55** (γ-hold global, not per-knob; KillSwitch unprotected).
- **A-56** (D-Bus writer no reconnect/retry/confirm — larger scope; parallels PR-URGENT-20 but for writer).
- **A-58** (event channel stalls runtime indefinitely — partially PR-URGENT-15).
- **A-60** (CallMyenergi `tokio::spawn` without timeout).

### Nits / doc
- A-37 (SPEC §7 writes_enabled default), A-53, A-62, A-63, A-64, A-67, A-68.
- PR-DAG-C — remaining semantic `depends_on` edges.
- PR-SCHED0-R2-D04 — double Publish(ActuatedPhase) dedup on Actuated<V>.
- PR-CADENCE-D2 — initial seed parallelization / outer budget.
- PR-URGENT-13-D03..D09 — review residuals.
- PR-DAG-A-R2-I01..I03 — informational notes.

## Blockers at session end

None. The schedule_0 blocker is resolved (PR-UX-1 + PR-sched-decisions). Rebuild + redeploy the new binary to see the dashboard show `ENABLED 02:00–05:00 soc=80%` on the schedule_0 actuated row and "Schedule 0 = Boost window 02:00–05:00, unconditionally ENABLED (days=7)" in the Decision panel.

## Recommended next session priorities

1. **PR-09b** — `grid_export_limit_w` hardening bundle (A-09, A-10, A-34/A-35). Straightforward boundary-validation fixes; needed before flipping `writes_enabled=true` on the export cap.
2. **A-17** — Fold Hoymiles EV-branch export into `solar_export` per SPEC §5.8. Plumbing work.
3. **A-29 / A-57 / A-65** — Schedule-writer atomicity + type assumption. Needed before live schedule writes.
4. **A-56** — D-Bus writer reconnect/retry. Parallels PR-URGENT-20 but on the writer side. Needed before live mode.
5. **A-13 + A-14** — Zappi night auto-stop (session_charged_pct plumbing + unit decision). A-14 is the blocker — ask user whether `zappi_limit` should be `%` or `kWh` per SPEC.

## Commits

```
1afc73d PR-forecast-backoff: classify HTTP failures + adaptive backoff (A-28)
ad8b25c PR-solcast-schema: distinguish schema drift from truly zero forecast (A-26)
a925147 PR-HYGIENE-2: reject invalid numeric input (A-25, A-41, A-48, A-51)
61cfee8 PR-forecast-freshness: real is_fresh predicate for forecast fusion (A-16)
3d9c987 PR-11: weather_soc γ-hold + once-per-day (A-20, A-21)
5f92960 PR-sched-decisions: split Schedule 0 / Schedule 1 decisions
aab6c28 PR-03: zappi_last_change_signature → monotonic Instant (A-04, A-24)
bf7b596 PR-UX-1: render schedule rows with explicit ENABLED/DISABLED
```
