{
  "name": "victron-controller-dashboard-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --outfile=../crates/shell/static/bundle.js",
    "watch": "esbuild src/index.ts --bundle --watch --outfile=../crates/shell/static/bundle.js --sourcemap",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
