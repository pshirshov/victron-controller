/target
**/*.rs.bk
.direnv/
result
result-*

# Host-specific config — filled in on the GX, never committed
/config.toml

# Discovery artifacts live under legacy/discovery-*
legacy/discovery-*/

# Web frontend build artifacts
web/node_modules/
web/dist/
# The built bundle IS checked in so `cargo build` works without a
# prior `npm run build` (needed by install-victron.sh and CI).
# If you want to regenerate, run `./scripts/build-web.sh`.
