/target
**/*.rs.bk
.direnv/
result
result-*

# Host-specific config — filled in on the GX, never committed
/config.toml

# Discovery artifacts live under legacy/discovery-*
legacy/discovery-*/

# Web frontend build artifacts
web/node_modules/
web/dist/
# The bundle is built by the `web-bundle` Nix derivation (or by
# ./scripts/build-web.sh for dev iteration). Not tracked in git.
crates/shell/static/bundle.js
.claude/
